# LaunchWatch Environment Variables
# Copy this file to .env.local and fill in your values

# NASA API Key (Optional)
# Get a free key at: https://api.nasa.gov
# Without this key, the app uses DEMO_KEY (rate-limited to 30 requests/hour)
# Free tier allows 1000 requests/hour
NEXT_PUBLIC_NASA_API_KEY=DEMO_KEY

# YouTube API Key (Optional)
# Get a free key at: https://console.cloud.google.com/apis/credentials
# Used for enhanced livestream detection and video metadata
# Without this, the app uses direct search URLs as fallback
NEXT_PUBLIC_YOUTUBE_API_KEY=

# Application Settings (Optional)
# Set to 'production' for production builds
NODE_ENV=development

# Analytics (Optional)
# Uncomment if using Vercel Analytics
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
